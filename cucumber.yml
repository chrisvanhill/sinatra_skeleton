default: --tags ~@selenium -r features/support -r features/step_definitions --format pretty features
autotest: --tags ~@selenium --drb -r features/support -r features/step_definitions --format progress features
selenium: --tags @selenium -r features/support -r features/step_definitions --format progress features